# Kubernetes Manifests (Placeholder)

This folder contains placeholder Kubernetes manifests for G4.

- `base/namespace.yaml` is a minimal namespace manifest used by CI linting.
- Add deployments, services, and HPAs as G4 services are added.
