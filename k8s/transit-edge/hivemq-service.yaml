#  hivemq-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: hivemq-service
  namespace: transit-edge
spec:
  type: ClusterIP
  ports:
    - port: 1883
      targetPort: 1883
      name: mqtt
    - port: 8883
      targetPort: 8883
      name: mqtts
  selector:
    app: hivemq